// Firebase retirado de la capa de datos.
// Compatibilidad temporal para imports antiguos.
export const db = { provider: 'postgresql' };
export const auth = null;
export const provider = null;
export default { provider: 'postgresql' };
